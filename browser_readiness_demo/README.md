# Browser-only public readiness demo

This static demo helps a facilitator explain the Action Acceptance Readiness Index in a browser without network calls. It is intentionally educational: it does not verify evidence, issue receipts, decide actions, certify vendors, or approve deployment.

Open `index.html` locally. No API keys, no backend, no telemetry, and no external network dependency.
